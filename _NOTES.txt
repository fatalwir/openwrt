config device
        option name 'br-lan'
        option type 'bridge'
        list ports 'eth0.1'

config interface 'lan'
        option device 'br-lan'
        option proto 'static'
        option ipaddr '192.168.1.1'
        option netmask '255.255.255.0'
        option ip6assign '60'

config interface 'lan_dhcp'
        option device 'br-lan'
        option proto 'dhcp'


scp bin/targets/ramips/mt76x8/openwrt-ramips-mt76x8-hilink_hlk-7628n-squashfs-sysupgrade.bin  root@192.168.33.198:/tmp


./scripts/feeds update -a
./scripts/feeds install -a
make menuconfig
make -j16 defconfig download clean world



TODO:
    TEST check why there is no I2C in the /proc/device-tree (maybe because the pin group was set to gpio mode?)
    TEST slcand if it works automatically
    TEST if two processes can use can0 simultaneously
    TEST - CHECK IF UCI DEFAULTS WORK (static ip + dhcp client, hostname) (target/linux/ramips/base-files/etc/uci-defaults/50_hapcan)
    make testing package in C using the interface (eth hapcan <-> can hapcan)
    avahi/mdns misto dualni ip konfigurace?
    remove KCONFIG comment in the mt76x8.mk

